{% extends 'base.html.twig' %}

{% block title %}Comentario{% endblock %}

{% block body %}
    <h1>Comentario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comentario.id }}</td>
            </tr>
            <tr>
                <th>CriadoEm</th>
                <td>{{ comentario.criadoEm ? comentario.criadoEm|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ comentario.descricao }}</td>
            </tr>
            <tr>
                <th>TipoOperacao</th>
                <td>{{ comentario.tipoOperacao }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comentario_index') }}">back to list</a>

    <a href="{{ path('app_comentario_edit', {'id': comentario.id}) }}">edit</a>

    {{ include('comentario/_delete_form.html.twig') }}
{% endblock %}
